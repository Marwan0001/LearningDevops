language: generic
os: linux
dist: focal

services:
  - docker
before_install:
  - docker build -t marwan/reactdev -f Dockerfile.dev .
script:
  - docker run -e CI=true marwan/reactdev npm test -- --watchAll=false
